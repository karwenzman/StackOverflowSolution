<UserControl x:Class="SampleNotifyPropertyChanged.Views.CustomControls.NumericTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SampleNotifyPropertyChanged.Views.CustomControls" 
             xmlns:viewmodels="clr-namespace:SampleNotifyPropertyChanged.ViewModels" 
             x:Name="numericTextBox" 
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ViewModelBase}"
             d:DesignHeight="450" d:DesignWidth="800">

    <TextBox Text="{Binding NumericString, Mode=TwoWay, 
                    UpdateSourceTrigger=PropertyChanged, 
                    ValidatesOnNotifyDataErrors=True,
                    RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}}"
             PreviewKeyDown="NumericTextBox_PreviewKeyDown"
             PreviewTextInput="NumericTextBox_PreviewTextInput" />

</UserControl>
